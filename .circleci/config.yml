version: 2.1
jobs:
    trial:
      docker:
            - image: circleci/ruby:2.5.3
      parallelism: 1
      steps:
        - run:
            name: Install Dependencies
            command: |
              lscpu
              echo i wan to

workflows:
    version: 2.1
    build:
        jobs:
        - trial
